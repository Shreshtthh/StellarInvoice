{
  "buildCommand": "cd packages/invoice_registry && npm install && npm run build && cd ../.. && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "vite",
  "outputDirectory": "dist"
}
